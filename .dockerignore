.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.lock

# Secrets — mount at runtime, never bake into image
.env
*.pfx

# Logs
*.log

# Docs
docs/
README.md

# Git
.git/
.gitignore

# Data / catalogs — never in build context (Dockerfile only COPYs pyproject + src/)
catalog/
catalog_pre_*/
results/
*.parquet
