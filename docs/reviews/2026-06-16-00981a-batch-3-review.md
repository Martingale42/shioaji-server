# Review — Batch 3: 00981A resume-fetch cron + BACKLOG row

> Reviewer pass over Batch 3 of the 00981A Top-300 Constituent Catalog pipeline.
> Branch `feat/00981a-universe`. Plan `docs/plans/2026-06-16-00981a-constituents-catalog.md` Tasks 5 + 6.

## Verdict: **APPROVED WITH NOTES**

Both commits are faithful, in-scope, and functionally correct. The cloned resume
script is an exact clone of `resume_0050_fetch.sh` with exactly the three intended
kinds of change, all invariants preserved, executable, and `bash -n`-clean. The
BACKLOG row BL-9 follows the file's schema and correctly flags the download as
user-coordinated and the crontab line as pending sign-off. The single note is a
**Minor** one: two cloned-from-0050 comment lines in the new script remain factually
wrong for the 00981A universe (carried over verbatim, untouched by the executor —
this is a faithful clone that simply inherited stale prose). No Critical or Important
findings.

---

## Commits reviewed

| Hash | Subject | Files | +/- |
|---|---|---|---|
| `b2cd0a8` | Add 00981A constituent resume-fetch cron script | `scripts/resume_00981a_fetch.sh` (new) | +60 |
| `87ea516` | Track 00981A constituent catalog download in backlog | `docs/BACKLOG.md` | +15 |

`git show --stat` confirms only these two files changed across the batch
(`git diff --stat 0d669b2 HEAD` → exactly `docs/BACKLOG.md` +15 and
`scripts/resume_00981a_fetch.sh` +60). No other file touched.

---

## Findings

### Critical
None.

### Important
None.

### Minor

- **[scripts/resume_00981a_fetch.sh:4]** Comment says *"re-runs `shioaji-data
  fetch-bars` over the full **81-code** union"*. 81 is the 0050 universe size; the
  00981A universe is the top-300 union (~330–380 codes per the plan File Structure
  table and Task 4). This line is a verbatim carry-over from
  `resume_0050_fetch.sh:4` — the executor did NOT alter it, consistent with the
  Task 5 instruction to "change only" CODES / LOGDIR / banners. Cosmetic / doc
  accuracy only; the functional code (`--codes-file "$CODES"`) reads the correct
  300-code file regardless. Suggest updating "81-code" → "top-300" in a follow-up.
- **[scripts/resume_00981a_fetch.sh:14-16]** Comment names *"Delisted members 2823
  中壽 / 2888 新光金 are handled separately"* — these are the **0050** delisted
  members (tracked in BL-8), not necessarily the 00981A delisted set. Also a
  verbatim 0050 carry-over (`resume_0050_fetch.sh:14-16`). Stale prose in the
  00981A script; no functional impact. Suggest genericising or removing in a
  follow-up.

> Both Minor notes stem from the Task 5 instruction scoping the edit to "only" the
> three change kinds, which deliberately did not include the body comments. The
> executor followed the literal instruction faithfully; the stale prose is a
> consequence of the clone-verbatim approach, not a divergence or defect.

---

## Checklist results

### 1. Faithful clone (Task 5) — PASS

`diff scripts/resume_0050_fetch.sh scripts/resume_00981a_fetch.sh` (exit 1 = files
differ, as expected):

```diff
2c2
< # Daily resume + verify for the 0050 point-in-time constituent universe download.
---
> # Daily resume + verify for the 00981A point-in-time constituent universe download.
25c25
< CODES="$REPO/universe/0050_constituents.txt"
---
> CODES="$REPO/universe/00981a_top300_constituents.txt"
28c28
< LOGDIR="$HOME/.shioaji-server/logs/0050_fetch"
---
> LOGDIR="$HOME/.shioaji-server/logs/00981a_fetch"
38c38
<   echo "0050 constituent resume run @ $(date -Is)"
---
>   echo "00981A constituent resume run @ $(date -Is)"
```

**Only the 3 intended kinds of change are present:**
1. `CODES` → `universe/00981a_top300_constituents.txt` (line 25). ✓
2. `LOGDIR` → `~/.shioaji-server/logs/00981a_fetch` (line 28). ✓
3. `0050` → `00981A` log/purpose banners (lines 2, 38). ✓

(The line-2 purpose banner is grouped with the banner change kind — it is a
`0050` → `00981A` label edit, intended.)

**Invariants verified UNCHANGED:**

| Invariant | Line | Status |
|---|---|---|
| `--start 2020-03-02` | 48 | UNCHANGED ✓ |
| `--concurrency 4` | 48 | UNCHANGED ✓ |
| Quota probe `curl … /api/account/usage` (before + after) | 41, 52 | UNCHANGED ✓ |
| `--catalog "$CATALOG"`, `CATALOG="$REPO/catalog"` (shared catalog) | 26, 47 | UNCHANGED ✓ |
| `--gateway-url "$GATEWAY"`, `--codes-file "$CODES"` | 45-46 | UNCHANGED ✓ |
| `inspect --catalog "$CATALOG"` | 55 | UNCHANGED ✓ |
| `set -uo pipefail`, PATH export, `cd "$REPO"` guard | 20, 22, 34 | UNCHANGED ✓ |

No unintended change to any code path, catalog target, or quota guard.

**Executable + syntax:**
- `ls -l scripts/resume_00981a_fetch.sh` → `-rwxrwxr-x` (executable bit set). ✓
- `bash -n scripts/resume_00981a_fetch.sh` → exit 0 (syntax OK). ✓

### 2. No silent divergence — PASS

- `CODES` points at `universe/00981a_top300_constituents.txt` — NOT 0050's
  `universe/0050_constituents.txt`. ✓
- `LOGDIR` points at `~/.shioaji-server/logs/00981a_fetch` — NOT 0050's
  `~/.shioaji-server/logs/0050_fetch`, so 0050 logs are not clobbered. ✓

No copy-paste miss that would fetch the wrong universe or overwrite 0050 logs.

### 3. Scope boundary respected — PASS

- **No crontab edit.** No crontab/cron file was committed or modified. The only
  "crontab" tokens in the diff are *documentation*: the script header comment
  (lines 14, 18-19, inherited from 0050) instructing the operator how to manage the
  cron line, and BL-9's note that the crontab line is *pending sign-off*. The cron
  line is documented as a string, never installed. ✓
- **No auto-download.** The script defines an idempotent resume that only runs when
  invoked; nothing in either commit triggers a fetch automatically (no `nohup`, no
  background launch, no `systemctl`, no `&`). ✓
- **No fake universe file.** `universe/00981a_top300_constituents.txt` does NOT
  exist on disk and is NOT tracked in git (`git ls-files` returns empty; `ls
  universe/` shows only the 0050 artifacts). The background fetch/build is still
  generating it, as expected. ✓
- `git show --stat` of both commits confirms only `scripts/resume_00981a_fetch.sh`
  and `docs/BACKLOG.md` changed. ✓

### 4. BACKLOG row BL-9 (Task 6) — PASS

BL-9 (`docs/BACKLOG.md:121-132`) conforms to the file's existing schema:
- **Numbering/heading:** continues the sequence (BL-8 → BL-9); heading format
  `## BL-9 ·〔Medium〕… — ⏸️ Open` matches the `## BL-N ·〔Severity〕title — status`
  convention used by BL-1…BL-8 (e.g. BL-8 `… — ⏸️ Deferred`). ✓
- **Columns/rows:** uses the same labelled bullets as peers —
  `- [ ] **狀態**`, `**Repo / branch**`, `**類型**`, `**背景**`, `**處置**`,
  `**驗收**`, `**參考**`. ✓
- **Status vocabulary:** `Open (operational / user-coordinated)` with the `⏸️`
  marker, consistent with BL-8's `Deferred` / `⏸️` and the `[ ]` unchecked box for
  pending items. ✓
- **User-coordination flagged:** "下載量約 7GB … 約 15 天工作"; "**與實盤交易共用
  Shioaji 每日配額**"; pre-run quota check `curl /api/account/usage` (lines 128-129). ✓
- **Crontab pending sign-off:** "crontab 行 `5 14 * * * …` **待使用者簽核**才寫入"
  (line 130). ✓
- **Links to plan + design:** `docs/plans/2026-06-16-00981a-constituents-catalog.md`
  (Task 6) + `…-design.md` (§B / 每日配額) (line 132). ✓
- **Existing rows undisturbed:** BL-1…BL-8 unchanged; the diff is a pure +15-line
  append (`git show 87ea516` shows only additions). ✓

---

## Verification log

- `diff scripts/resume_0050_fetch.sh scripts/resume_00981a_fetch.sh` → exit 1
  (4 hunks, all intended; pasted above).
- `bash -n scripts/resume_00981a_fetch.sh` → exit 0.
- `ls -l scripts/resume_00981a_fetch.sh` → `-rwxrwxr-x` (executable).
- `test -f universe/00981a_top300_constituents.txt` → absent; `git ls-files` → empty.
- `git diff --stat 0d669b2 HEAD` → only `docs/BACKLOG.md` (+15) and
  `scripts/resume_00981a_fetch.sh` (+60).
- `uv run pytest` NOT run (per review instructions: env blocked by pre-existing
  nautilus wheel hash mismatch; no Python changed this batch).
- Resume script NOT executed (per review instructions: it is the quota-limited
  operational run, gated on user sign-off).
